import VerifyTokenClient from './VerifyTokenClient';

export const metadata = {
  title: 'Verify credential',
  robots: { index: false, follow: false },
};

export default function VerifyTokenPage() {
  return <VerifyTokenClient />;
}
